package logic.table;
/**
 * class that represent a null table, this class apriori shouldnt exist
 * bucause the metod information of a null table is in general table,
 * but the program send some erros when the table was suposed to be observed
 *
 * @author David de la puente
 */
public class NullGameTable extends GeneralTable {
}
